
export class EnemyEmptyBehaviour {
  update(sprite, player) {
    // No updates to do
  }

  platformCollision(sprite, player) {
    // No collions to handle
  }

  playerCollision(sprite, player) {
    // No collisions to handle
  }
}
